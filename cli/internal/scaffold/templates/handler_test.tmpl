package http_test

import (
    "net/http"
    "net/http/httptest"
    "strings"
    "testing"

    "{{ .ImportPath }}/internal/handler/http"
)

func Test{{ .EntityName }}Handler_Create(t *testing.T) {
    handler := http.New{{ .EntityName }}Handler(nil) // use mocks in real test

    req := httptest.NewRequest(http.MethodPost, "/{{ .PluralKebab }}", strings.NewReader(`{}`))
    rr := httptest.NewRecorder()

    handler.Create(rr, req)

    if rr.Code != http.StatusOK {
        t.Errorf("expected 200, got %d", rr.Code)
    }
}
